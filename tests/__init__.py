"""Test package for the Financial Report Agent.

Plain-assert test modules (no framework). Run from the project root:

    ./.venv/bin/python -m tests               # the whole suite
    ./.venv/bin/python -m tests.test_report   # one module

Running as `-m` puts the project root on sys.path, so the tests import the
app's modules (config, research, ...) directly.
"""
